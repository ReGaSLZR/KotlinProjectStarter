<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="@dimen/toolbar_height"
        android:background="@color/colorPrimary">

    <ImageView
            android:id="@+id/toolbar_imageView"
            android:layout_width="@dimen/toolbar_height"
            android:layout_height="@dimen/toolbar_height"
            android:background="@drawable/drawable_toolbar_icon_background"
            android:paddingBottom="@dimen/margin_padding_small"
            android:paddingTop="@dimen/margin_padding_small"
            android:src="@drawable/drawable_ic_back_white"
            android:visibility="visible" />

    <TextView
            android:id="@+id/toolbar_textView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/toolbar_height"
            android:layout_marginRight="@dimen/toolbar_height"
            android:foregroundGravity="center"
            android:gravity="center"
            android:textAlignment="center"
            tools:text="Test Textview Title"
            style="@style/text_highlight" />

</android.support.v7.widget.Toolbar>